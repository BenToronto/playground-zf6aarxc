# Welcome!

This C# template lets you get started quickly with a simple one-page playground.

```C# runnable
// { autofold
using System;

class Hello 
{
    static void Main() 
    {
// }

private int myVar;
public int MyProperty
{
    get { return myVar; }
    set { myVar = value; }
}

public class Property : get { return new Property};

goto()

set base;
get { return

fixed null(

    as break;

    private class Property : Propertyp
    public int MyProperty { get; set; }

    get { return my}

    base;

    public Property()
)}

if (null int)
public int MyProperty
int my = 0;

EventHandler temp = MyEvent;
if (temp != null)
{
    temp();
    try myVar;


    
}

static set[
    EventHandler temp();
]

abstract my var;

EventHandler temp = null;

static int Main(string[] args)
{
    EventHandler();
    return 0;
}

get temp;

abstract;
private i myVar;
public i MyProperty
{
    get { return myVar; }
    set { myVar = value; }
}
if fixed try;

get;

EventHandler temp = MyEvent = 10
EventHandler temp;

EventHandler;

public int MyProperty;

EventHandler temp;


abstract private;

get lock;
EventHandler temp


for (int f = 0; f < length; f++)
{
    class Property : my

    EventHandler
}

get;
set;


public int MyProperty
value = 5
return 0;
static int Main(string[] args)
{
    static int Main(string[] args)
    {
        static int Main(string[] args)
        {
            static int Main(string[] args)
            {
                
                return 0;
            }
            return 0;
        }
        return 0;
    }
    return 0;
}

EventHandler temp;

// { autofold
    }
}
// }
```

# Advanced usage

If you want a more complex example (external libraries, viewers...), use the [Advanced C# template](https://tech.io/select-repo/386)
